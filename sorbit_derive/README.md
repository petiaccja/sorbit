# Sorbit derive macros

This crate contains the derive macros used by [sorbit](https://github.com/petiaccja/sorbit), and is not meant to be used on its own.

This crate is an extremely thin wrapper around the `sorbit_derive_impl` crate, exposing the functionality as a procedural macro crate.